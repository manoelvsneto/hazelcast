apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: hazelcast-tls
  namespace: hazelcast
spec:
  secretName: hazelcast-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - hazelcast.archse.eng.br
